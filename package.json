{
  "name": "lerna-test",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "add:ui": "lerna add ui-components --scope=webapp",
    "start:webapp": "yarn workspace webapp dev",
    "build:ui": "yarn workspace ui-components build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  }
}
