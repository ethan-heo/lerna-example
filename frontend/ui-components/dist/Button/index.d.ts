import React from "react";
declare type Props = {
    children: React.ReactNode;
    className?: string;
};
declare function Button({ children, className }: Props): JSX.Element;
export default Button;
